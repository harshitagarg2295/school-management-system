<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fees Page : <%= student.name %>
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
    <link rel="stylesheet" href="/adminDashboard.css">
</head>

<style>
    /* --- Reset & Basics --- */
    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    body {
        background-color: #f1f1f1;
        font-family: sans-serif;
    }

    .student-dash {
        display: flex;
        min-height: 100vh;
    }

    /* --- Main Card Container --- */
    .fees-card {
        flex: 1;
        background-color: rgb(184, 213, 224);
        padding: 30px 25px;
        margin: 20px;
        border-radius: 8px;
        max-width: 100%;
    }

    .fees-card h2 {
        font-size: 28px;
        text-align: center;
        margin: 30px 0px;
    }

    /* --- Student Details Section --- */
    .student-details {
        display: flex;
        justify-content: space-between;
        margin: 0px 30px;
        flex-wrap: wrap;
    }

    .student-details p {
        margin: 15px 0px;
        font-size: 18px;
    }

    hr {
        border: 0;
        border-top: 1px solid #9fbcc9;
        margin: 20px 0;
    }

    /* --- Header Area --- */
    .fee-structure-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        margin-bottom: 20px;
    }

    .fee-structure-header h1 {
        margin: 10px;
    }

    .pay-fee-btn {
        background-color: rgb(66, 154, 242);
        color: white;
        font-size: 16px;
        padding: 10px 20px;
        border-radius: 8px;
        outline: none;
        border: none;
        cursor: pointer;
        text-decoration: none;
        display: inline-block;
    }

    /* --- Desktop Table Layout (Grid) --- */
    .fee-header {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr 1fr 1.2fr 1fr;
        background-color: rgb(81, 155, 183);
        border-radius: 8px 8px 0 0;
        text-align: center;
        font-size: 17px;
        padding: 15px 10px;
        font-weight: 500;
    }

    .fee-content {
        display: flex;
        flex-direction: column;
    }

    .fee-row {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr 1fr 1.2fr 1fr;
        padding: 15px 10px;
        background-color: #f9f9f9;
        border-bottom: 1px solid #ddd;
        align-items: center;
        text-align: center;
    }

    .fee-row:last-child {
        border-radius: 0 0 8px 8px;
        border-bottom: none;
    }

    .fee-row p {
        font-size: 16px;
        margin: 0;
        word-wrap: break-word;
    }

    /* --- Status Badges --- */
    .status {
        display: inline-flex;
        align-items: center;
        align-items: center;
        justify-content: center;
        padding: 6px 12px;
        border-radius: 20px;
        font-size: 14px;
        font-weight: 600;
        min-width: 90px;
    }

    .status.paid {
        background-color: #e0f8f1;
        color: #1aaa55;
    }

    .status.pending {
        background-color: #fff1e5;
        color: #e67817;
    }

    .status .dot {
        width: 8px;
        height: 8px;
        border-radius: 50%;
        background-color: currentColor;
        margin-right: 6px;
    }


    @media (max-width: 850px) {
        .fee-header {
            display: none;
        }

        .fee-row {
            display: flex;
            flex-direction: column;
            background-color: white;
            margin-bottom: 15px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border: 1px solid #eee;
        }

        .fee-row p {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            margin-bottom: 10px;
            font-size: 16px;
            text-align: right;
            border-bottom: 1px solid #f0f0f0;
            padding-bottom: 5px;
        }

        .fee-row p:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }

        .fee-row p:nth-child(1)::before {
            content: "Fee Type :";
            font-weight: bold;
            color: #555;
        }

        .fee-row p:nth-child(2)::before {
            content: "Installment :";
            font-weight: bold;
            color: #555;
        }

        .fee-row p:nth-child(3)::before {
            content: "Amount :";
            font-weight: bold;
            color: #555;
        }

        .fee-row p:nth-child(4)::before {
            content: "Status :";
            font-weight: bold;
            color: #555;
        }

        .fee-row p:nth-child(5)::before {
            content: "Date :";
            font-weight: bold;
            color: #555;
        }

        .fee-row p:nth-child(6)::before {
            content: "Mode :";
            font-weight: bold;
            color: #555;
        }

        .student-details {
            flex-direction: column;
            gap: 0;
        }

        .fees-card {
            padding: 15px;
            margin: 10px;
        }

        .pay-fee-btn {
            width: 100%;
            text-align: center;
            margin-top: 10px;
        }

        .vfee-row p:nth-child(1)::before {
            content: "Installment :";
            font-weight: bold;
            color: #555;
        }

        .vfee-row p:nth-child(2)::before {
            content: "Month :";
            font-weight: bold;
            color: #555;
        }

        .vfee-row p:nth-child(3)::before {
            content: "Amount :";
            font-weight: bold;
            color: #555;
        }

        .vfee-row p:nth-child(4)::before {
            content: "Status :";
            font-weight: bold;
            color: #555;
        }

        .vfee-row p:nth-child(5)::before {
            content: "Date :";
            font-weight: bold;
            color: #555;
        }

        .vfee-row p:nth-child(6)::before {
            content: "Mode :";
            font-weight: bold;
            color: #555;
        }
    }

    @media(max-width: 550px) {
        .fee-structure-header {
            justify-content: center;
        }
    }
</style>

<body>
    <div class="student-dash">

        <%- include("partials/StudentPageSidebar") %>

            <div class="fees-card">

                <h2><strong>Student Name : </strong>
                    <%= student.name %>
                </h2>

                <div class="student-details">
                    <div class="school-related-details">
                        <p><strong>Student ID :</strong>
                            <%= student.id %>
                        </p>
                        <p><strong>Class :</strong>
                            <%= student.class %>
                        </p>
                        <p><strong>Total Fees :</strong> ₹<%= student.fees.toLocaleString('en-IN') %>
                        </p>
                    </div>

                    <div class="basic-details">
                        <p><strong>Phone :</strong>
                            <%= student.phone %>
                        </p>
                        <p><strong>Date of Birth :</strong>
                            <%= student.DOB ? new Date(student.DOB).toLocaleDateString('en-GB') : 'N/A' %>
                        </p>
                        <p><strong>Address :</strong>
                            <%= student.address %>
                        </p>
                    </div>
                </div>
                <hr>

                <div class="fee-structure">
                    <div class="fee-structure-header">
                        <h1>Fee Structure</h1>
                        <a href="/students/checkout-fees" class="pay-fee-btn" style="text-decoration: none;">Pay Online
                            Fees</a>
                    </div>

                    <div class="fee-header">
                        <span>Fee Type</span>
                        <span>Installment</span>
                        <span>Amount</span>
                        <span>Status</span>
                        <span>Date of Payment</span>
                        <span>Mode</span>
                    </div>

                    <div class="fee-content">
                        <% student.feeStatus.forEach((fee, i)=> { %>
                            <div class="fee-row" id="fee-row-<%= i %>">
                                <p>
                                    <%= fee.feeType %>
                                </p>

                                <p>
                                    <%= fee.installment %>
                                </p>

                                <p>₹<%= fee.amount.toLocaleString('en-IN') %>
                                </p>

                                <p>
                                    <% if (fee.status==='Paid' ) { %>
                                        <span class="status paid"><span class="dot"></span> Paid</span>
                                        <% } else { %>
                                            <span class="status pending"><span class="dot"></span> Pending</span>
                                            <% } %>
                                </p>

                                <p>
                                    <%= fee.paymentDate ? new Date(fee.paymentDate).toLocaleDateString("en-GB") : "-" %>
                                </p>

                                <p>
                                    <%= fee.mode ? fee.mode : '-' %>
                                </p>
                            </div>
                            <% }) %>
                    </div>
                </div>
                <!-- ===== VEHICLE FEES SECTION ===== -->
                <% if (student.isVehicleAssigned && student.vehicleFeeStatus && student.vehicleFeeStatus.length> 0) { %>
                    <hr>
                    <div class="fee-structure">
                        <div class="fee-structure-header">
                            <h1>🚌 Vehicle Fee Structure</h1>
                            <span style="font-size:15px; color:#2e6b4f; font-weight:600;">
                                Annual: ₹<%= (student.vehicleDetails && student.vehicleDetails.vehicleFees) ?
                                    student.vehicleDetails.vehicleFees.toLocaleString('en-IN') : '0' %>
                            </span>
                        </div>
                        <div class="fee-header" style="background-color: rgb(74, 154, 90);">
                            <span>Installment</span>
                            <span>Month</span>
                            <span>Amount</span>
                            <span>Status</span>
                            <span>Date of Payment</span>
                            <span>Mode</span>
                        </div>
                        <div class="fee-content">
                            <% student.vehicleFeeStatus.forEach((fee, i)=> { %>
                                <div class="fee-row vfee-row" id="vfee-row-<%= i %>">
                                    <p>
                                        <%= fee.installment %>
                                    </p>
                                    <p>
                                        <%= fee.month %>
                                    </p>
                                    <p>₹<%= fee.amount.toLocaleString('en-IN') %>
                                    </p>
                                    <p>
                                        <% if (fee.status==='Paid' ) { %>
                                            <span class="status paid"><span class="dot"></span> Paid</span>
                                            <% } else if (fee.status==='Partial' ) { %>
                                                <span class="status pending"><span class="dot"></span> Partial</span>
                                                <% } else { %>
                                                    <span class="status pending"><span class="dot"></span>
                                                        Pending</span>
                                                    <% } %>
                                    </p>
                                    <p>
                                        <%= fee.paymentDate ? new Date(fee.paymentDate).toLocaleDateString("en-GB")
                                            : "-" %>
                                    </p>
                                    <p>
                                        <%= fee.mode ? fee.mode : '-' %>
                                    </p>
                                </div>
                                <% }) %>
                        </div>
                    </div>
                    <% } %>
            </div>
    </div>
    <script src="/backButton.js"></script>

</body>

</html>